export type UnauthenticatedLayoutProps = {
  children: React.ReactNode;
  /**
   * The slogan of the auth page.
   */
  slogan: string;
  /**
   * The title of the auth page.
   */
  title: string;
  /**
   * The subtitle of the auth page.
   */
  subtitle?: string;
};
